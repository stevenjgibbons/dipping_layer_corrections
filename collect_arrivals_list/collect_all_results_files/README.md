
A folder in which to collect all the processed day files into a single file per station.  
The python script **make_station_all_arrivals.py** does the work, driven by **run_all.sh** and put into the background using **bg_run.sh**.
